import { ROUTES } from './routes';
import { INPUT_RULES, PASSWORD_RULES } from './validate';

export const USE_PROGRESS = 'Use-Progress';
export const YES = 'YES';
export const NO = 'NO';
export const ALL = '_all_';
export const NEW = '_new_';

export { ROUTES, INPUT_RULES, PASSWORD_RULES };
